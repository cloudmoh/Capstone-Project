version: "3"

services:
  react-container:
    build:
      context: ./app
      dockerfile: Dockerfile
    image: capstone-image
    container_name: my-capstone-react-app-container
    ports:
      - "80:80"
    volumes:
      - ./src:/app/src
